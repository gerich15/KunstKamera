#!/bin/bash

# Скрипт запуска приложения на Beget

echo "🚀 Запуск Kunstkamera..."

# Переходим в директорию проекта
cd "$(dirname "$0")"

# Проверяем наличие .env файла
if [ ! -f .env.local ]; then
    echo "❌ Файл .env.local не найден!"
    echo "Создайте файл .env.local с переменными окружения"
    exit 1
fi

# Проверяем установку зависимостей
if [ ! -d "node_modules" ]; then
    echo "📦 Установка зависимостей..."
    npm install --production
fi

# Собираем проект (если нужно)
if [ ! -d ".next" ]; then
    echo "🔨 Сборка проекта..."
    npm run build
fi

# Запускаем приложение
echo "✅ Запуск приложения на порту 3000..."
npm start

