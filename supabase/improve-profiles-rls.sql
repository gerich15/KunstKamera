-- Улучшение RLS политик для profiles
-- Профили должны быть видны всем (для публичных страниц), но это нормально
-- Если нужно ограничить, можно использовать эту политику:

-- Удаляем старую политику (если нужно ограничить видимость)
-- DROP POLICY IF EXISTS "Пользователи могут видеть все профили" ON public.profiles;

-- Создаём более строгую политику (опционально)
-- CREATE POLICY "Пользователи могут видеть публичные профили"
--   ON public.profiles FOR SELECT
--   USING (true); -- Оставляем как есть, так как username нужен для публичных URL

-- Текущая политика корректна для MVP:
-- Профили видны всем, что необходимо для публичных страниц музеев
-- UPDATE и INSERT ограничены только своим профилем - это безопасно

