# 🔧 Исправление проблем на Netlify

## Проблема 1: Стили не применяются

### Причина
Next.js на Netlify требует специальной конфигурации для правильной работы со статическими файлами.

### Решение

1. **Убедитесь, что файл `netlify.toml` создан** (уже создан в репозитории)

2. **Проверьте настройки сборки в Netlify:**
   - Откройте **Site settings** → **Build & deploy** → **Build settings**
   - **Build command**: `npm run build`
   - **Publish directory**: `.next` (или оставьте пустым, если используете плагин)
   - **Node version**: `18` (в Environment variables добавьте `NODE_VERSION = 18`)

3. **Установите плагин Next.js для Netlify:**
   - В Netlify Dashboard: **Site settings** → **Plugins**
   - Найдите **"Essential Next.js Plugin"** или **"@netlify/plugin-nextjs"**
   - Установите его (если еще не установлен)

4. **Очистите кэш и пересоберите:**
   - **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

### Проверка

Откройте сайт и проверьте в консоли браузера (F12):
- Нет ли ошибок загрузки CSS файлов
- Загружаются ли файлы из `/_next/static/`

---

## Проблема 2: Регистрация не работает на мобильном

### Причина
Скорее всего проблема с переменными окружения или редиректами OAuth.

### Решение

#### Шаг 1: Проверьте переменные окружения

В Netlify Dashboard → **Site settings** → **Environment variables** должны быть:

✅ **NEXT_PUBLIC_SUPABASE_URL**
- Формат: `https://ваш-project-id.supabase.co`
- **Scopes**: Build и Runtime (или All scopes)

✅ **NEXT_PUBLIC_SUPABASE_ANON_KEY**
- Ваш anon key из Supabase
- **Scopes**: Build и Runtime (или All scopes)

✅ **NEXT_PUBLIC_SITE_URL** (рекомендуется)
- URL вашего сайта на Netlify: `https://your-site.netlify.app`
- **Scopes**: Build и Runtime

#### Шаг 2: Проверьте настройки Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в **Authentication** → **URL Configuration**
3. В **Redirect URLs** добавьте:
   ```
   https://your-site.netlify.app/**
   https://your-site.netlify.app/auth/callback
   ```
4. В **Site URL** укажите: `https://your-site.netlify.app`

#### Шаг 3: Проверьте логи в Netlify

1. Откройте **Functions** → **Logs** в Netlify Dashboard
2. Попробуйте зарегистрироваться
3. Проверьте, есть ли ошибки в логах

#### Шаг 4: Проверьте консоль браузера на мобильном

1. На мобильном устройстве откройте сайт
2. Откройте DevTools (если возможно) или используйте [Remote Debugging](https://developer.chrome.com/docs/devtools/remote-debugging/)
3. Проверьте консоль на ошибки:
   - Ошибки загрузки переменных окружения
   - Ошибки CORS
   - Ошибки аутентификации

---

## Общая диагностика

### Проверьте сборку

1. В Netlify Dashboard откройте последний деплой
2. Проверьте логи сборки на ошибки
3. Убедитесь, что сборка завершилась успешно

### Проверьте переменные окружения

```bash
# В логах сборки должны быть видны переменные (без значений)
# Если их нет - они не загружены
```

### Проверьте в браузере

Откройте консоль (F12) и выполните:

```javascript
// Проверьте переменные окружения
console.log('SUPABASE_URL:', process.env.NEXT_PUBLIC_SUPABASE_URL ? '✅' : '❌')
console.log('SUPABASE_KEY:', process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ? '✅' : '❌')

// Проверьте загрузку стилей
const stylesheets = Array.from(document.querySelectorAll('link[rel="stylesheet"]'))
console.log('CSS файлы:', stylesheets.length)
stylesheets.forEach(link => console.log(link.href))
```

---

## Быстрое исправление

### 1. Обновите конфигурацию

Убедитесь, что файл `netlify.toml` есть в репозитории и закоммичен:

```bash
git add netlify.toml
git commit -m "Add Netlify configuration"
git push
```

### 2. Пересоберите проект

В Netlify Dashboard:
- **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

### 3. Проверьте переменные окружения

Убедитесь, что все переменные добавлены с правильными scopes (Build и Runtime).

---

## Если ничего не помогает

### Вариант 1: Проверьте версию Next.js

Убедитесь, что используется совместимая версия. В `package.json` должна быть:
```json
"next": "14.0.4"
```

### Вариант 2: Используйте статический экспорт (не рекомендуется)

Если проблемы продолжаются, можно попробовать статический экспорт, но это ограничит функциональность:

```javascript
// next.config.js
module.exports = {
  output: 'export', // Только для статических сайтов
  // ...
}
```

### Вариант 3: Обратитесь в поддержку Netlify

Если проблемы не решаются, создайте тикет в [Netlify Support](https://www.netlify.com/support/) с:
- URL вашего сайта
- Скриншотами ошибок из консоли
- Логами сборки
- Описанием проблемы

---

## Чеклист проверки

- [ ] Файл `netlify.toml` создан и закоммичен
- [ ] Плагин Next.js установлен в Netlify
- [ ] Переменные окружения добавлены с правильными scopes
- [ ] `NEXT_PUBLIC_SITE_URL` указан правильно
- [ ] Redirect URLs настроены в Supabase
- [ ] Кэш очищен и проект пересобран
- [ ] Проверены логи сборки на ошибки
- [ ] Проверена консоль браузера на ошибки

