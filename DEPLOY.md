# 🚀 Как выложить Kunstkamera в общий доступ

Пошаговая инструкция по деплою на Netlify (бесплатный хостинг).

## 📋 Подготовка

### 1. Убедитесь, что код готов

```bash
# Проверьте, что всё работает локально
npm run build
```

Если сборка проходит успешно, можно деплоить!

### 2. Закоммитьте и запушьте код в GitHub

```bash
git add .
git commit -m "Готово к деплою"
git push origin main
```

## 🌐 Деплой на Netlify

### Шаг 1: Создайте аккаунт на Netlify

1. Перейдите на [netlify.com](https://www.netlify.com)
2. Нажмите **Sign up** (можно через GitHub)
3. Подтвердите email

### Шаг 2: Подключите репозиторий

1. В Netlify Dashboard нажмите **Add new site** → **Import an existing project**
2. Выберите **GitHub** и авторизуйтесь
3. Найдите ваш репозиторий `KunstKamera` и выберите его
4. Нажмите **Import**

### Шаг 3: Настройте сборку

Netlify автоматически определит Next.js, но проверьте настройки:

- **Build command**: `npm run build`
- **Publish directory**: `.next` (или оставьте пустым для Next.js)
- **Node version**: `18` или `20` (выберите в настройках)

### Шаг 4: Добавьте переменные окружения

**ВАЖНО!** Без этого приложение не будет работать!

1. В настройках проекта перейдите: **Site settings** → **Build & deploy** → **Environment**
2. Нажмите **Add variable** и добавьте:

#### Обязательные переменные:

**1. NEXT_PUBLIC_SUPABASE_URL**
- **Key**: `NEXT_PUBLIC_SUPABASE_URL`
- **Value**: ваш URL из Supabase (например: `https://hwgqzsziskhnqqbjpdvz.supabase.co`)
- **Scopes**: выберите **All scopes** (Build и Runtime)

**2. NEXT_PUBLIC_SUPABASE_ANON_KEY**
- **Key**: `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- **Value**: ваш anon key из Supabase
- **Scopes**: выберите **All scopes**

**Где найти эти значения:**
- Откройте [Supabase Dashboard](https://app.supabase.com)
- Выберите ваш проект
- Перейдите в **Settings** → **API**
- Скопируйте:
  - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
  - **anon public** ключ → `NEXT_PUBLIC_SUPABASE_ANON_KEY`

#### Опциональная переменная:

**3. NEXT_PUBLIC_SITE_URL** (рекомендуется)
- **Key**: `NEXT_PUBLIC_SITE_URL`
- **Value**: URL вашего сайта на Netlify (будет доступен после первого деплоя)
- **Scopes**: **All scopes**
- **Примечание**: Добавьте эту переменную после первого деплоя, когда узнаете URL сайта

### Шаг 5: Запустите деплой

1. После добавления переменных нажмите **Deploy site**
2. Дождитесь завершения сборки (обычно 2-5 минут)
3. После успешного деплоя вы получите URL вида: `https://random-name-123.netlify.app`

### Шаг 6: Обновите NEXT_PUBLIC_SITE_URL (если нужно)

1. После первого деплоя скопируйте URL вашего сайта
2. Вернитесь в **Environment variables**
3. Добавьте или обновите `NEXT_PUBLIC_SITE_URL` с вашим URL
4. Запустите новый деплой: **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

## ✅ Проверка после деплоя

Откройте ваш сайт и проверьте:

- [ ] Главная страница открывается
- [ ] Страница `/login` работает
- [ ] Можно зарегистрироваться
- [ ] Можно войти в аккаунт
- [ ] Можно создать музей
- [ ] Можно загрузить изображение
- [ ] Публичная страница музея открывается

## 🔧 Настройка кастомного домена (опционально)

1. В Netlify Dashboard: **Site settings** → **Domain management**
2. Нажмите **Add custom domain**
3. Введите ваш домен (например: `kunstkamera.com`)
4. Следуйте инструкциям по настройке DNS

## 📝 Что дальше?

После успешного деплоя:

1. **Поделитесь ссылкой** с друзьями и пользователями
2. **Настройте GitHub OAuth** (если используете):
   - В настройках GitHub OAuth App обновите Callback URL на ваш Netlify URL
   - В Supabase обновите Redirect URLs
3. **Мониторьте логи** в Netlify Dashboard → **Functions** → **Logs**

## 🐛 Решение проблем

### Ошибка "Missing Supabase environment variables"
- Проверьте, что переменные добавлены в Netlify Dashboard
- Убедитесь, что выбраны правильные scopes (Build и Runtime)
- Очистите кэш и пересоберите: **Deploys** → **Trigger deploy** → **Clear cache and deploy site**

### Ошибка при сборке
- Проверьте логи сборки в Netlify Dashboard
- Убедитесь, что `npm run build` проходит локально
- Проверьте версию Node.js в настройках (должна быть 18+)

### Сайт работает, но авторизация не работает
- Проверьте переменные окружения в Netlify
- Убедитесь, что в Supabase настроены правильные Redirect URLs
- Проверьте логи в Netlify Dashboard → **Functions** → **Logs**

## 📚 Полезные ссылки

- [Netlify Documentation](https://docs.netlify.com/)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Supabase Documentation](https://supabase.com/docs)

---

**Готово!** Ваше приложение теперь доступно в интернете! 🎉

