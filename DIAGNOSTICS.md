# 🔧 Диагностика проблем с авторизацией

Если вы получаете ошибку `401 (Unauthorized)` при попытке войти, выполните следующие шаги:

## 1. Проверка переменных окружения

### В браузере (консоль):
1. Откройте консоль разработчика (F12)
2. Введите: `checkSupabase()`
3. Проверьте вывод - должны быть ✅ для обеих переменных

### В файле `.env.local`:
Убедитесь, что файл существует в корне проекта и содержит:

```env
NEXT_PUBLIC_SUPABASE_URL=https://hwgqzsziskhnqqbjpdvz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_полный_anon_ключ_из_supabase
```

**ВАЖНО:**
- ❌ НЕ используйте кавычки вокруг значений
- ❌ НЕ добавляйте пробелы до/после `=`
- ✅ Ключ должен быть ПОЛНЫМ (обычно ~200+ символов)

### Где взять ключи:
1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Settings → **API**
4. Скопируйте:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public** key → `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## 2. Перезапуск сервера

**КРИТИЧНО:** После изменения `.env.local` ОБЯЗАТЕЛЬНО перезапустите сервер:

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

## 3. Проверка в Supabase Dashboard

### Authentication → Users
- Убедитесь, что ваш пользователь существует
- Если нет - зарегистрируйтесь через форму на `/login`

### Authentication → Providers → Email
- Для разработки: отключите **"Enable email confirmations"**
- Это позволит входить сразу после регистрации

## 4. Очистка браузера

Если проблема сохраняется:

1. Откройте DevTools (F12)
2. Application (или Storage) → Cookies
3. Удалите все cookies для `localhost:3000`
4. Application → Local Storage
5. Удалите все данные для `localhost:3000`
6. Обновите страницу (Ctrl+F5)

## 5. Проверка ошибок в консоли

После попытки входа проверьте консоль:

- ✅ `Sign in success: { user: "...", session: true }` - всё работает!
- ❌ `401 (Unauthorized)` - проблема с ключами или пользователем
- ❌ `Missing Supabase environment variables` - `.env.local` не загружен

## 6. Типичные ошибки

### "Invalid login credentials"
- Неверный email или пароль
- Пользователь не существует - зарегистрируйтесь

### "Email not confirmed"
- Включено подтверждение email
- Отключите в Supabase: Authentication → Providers → Email → "Enable email confirmations" = OFF

### "401 Unauthorized" на `/auth/v1/token`
- Неправильный `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- Проверьте, что ключ скопирован полностью
- Убедитесь, что сервер перезапущен после изменения `.env.local`

## 7. Если ничего не помогает

1. Проверьте, что используете правильный проект Supabase
2. Убедитесь, что проект активен (не приостановлен)
3. Попробуйте создать новый проект Supabase и перенести туда данные
4. Проверьте логи в Supabase Dashboard → Logs → Auth

