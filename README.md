# Kunstkamera - Digital Curiosity Cabinet

Платформа для создания, организации и демонстрации коллекций цифровых артефактов.

## 🚀 Технологический стек

- **Frontend**: Next.js 14 (App Router) + TypeScript + Tailwind CSS + shadcn/ui
- **Backend**: Next.js API Routes (через Supabase)
- **База данных**: Supabase (PostgreSQL)
- **Аутентификация**: Supabase Auth (Email/Password + GitHub OAuth)
- **Хранилище файлов**: Supabase Storage
- **Развертывание**: Vercel

## 📋 Возможности

- ✅ Создание и управление музеями (коллекциями)
- ✅ Добавление экспонатов: изображения, текст, ссылки, видео
- ✅ Публичные страницы музеев с уникальными URL
- ✅ Различные типы отображения: сетка, masonry, список
- ✅ Профили пользователей
- ✅ Система лайков (базовая)

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js 18+ и npm
- Аккаунт Supabase (бесплатный план подходит)

### Шаг 1: Клонирование и установка зависимостей

```bash
git clone <repository-url>
cd Kunstkamera
npm install
```

### Шаг 2: Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Перейдите в **Settings → API** и скопируйте:
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public key** → `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - **service_role secret key** → `SUPABASE_SERVICE_ROLE_KEY`

### Шаг 3: Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

**⚠️ ВАЖНО**: Никогда не коммитьте `.env.local` в Git!

### Шаг 4: Настройка базы данных

1. Откройте **SQL Editor** в Supabase Dashboard
2. Выполните скрипт `supabase/schema.sql` (создаёт таблицы, индексы, RLS политики)
3. Создайте Storage buckets:
   - Перейдите в **Storage → Buckets**
   - Создайте bucket `museum-covers` (Public: включено)
   - Создайте bucket `artifacts` (Public: включено)
4. Выполните скрипт `supabase/apply-storage-policies.sql` (политики доступа для Storage)

### Шаг 5: Настройка аутентификации (опционально)

#### Email/Password (работает из коробки)
- В **Authentication → Providers → Email** можно отключить подтверждение email для разработки

#### GitHub OAuth (опционально)
1. Создайте **OAuth App** на GitHub (Settings → Developer settings → OAuth Apps)
2. Callback URL: `https://your-project.supabase.co/auth/v1/callback`
3. Скопируйте **Client ID** и **Client Secret**
4. В Supabase: **Authentication → Providers → GitHub**
   - Включите провайдер
   - Вставьте Client ID и Client Secret

### Шаг 6: Запуск приложения

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Структура проекта

```
Kunstkamera/
├── app/                    # Next.js App Router страницы
│   ├── dashboard/         # Личный кабинет
│   ├── museum/            # Редактирование музеев
│   ├── public/            # Публичные страницы музеев
│   ├── profile/           # Профиль пользователя
│   ├── login/             # Авторизация
│   ├── error.tsx          # Глобальная страница ошибок
│   ├── loading.tsx         # Глобальный лоадер
│   ├── not-found.tsx      # 404 страница
│   ├── robots.ts          # robots.txt
│   └── sitemap.ts         # sitemap.xml
├── components/            # React компоненты
│   ├── ui/               # shadcn/ui компоненты
│   └── ...
├── lib/                  # Утилиты и конфигурация
│   ├── supabase/         # Supabase клиенты
│   ├── validation.ts     # Zod схемы
│   └── utils.ts          # Утилиты
├── hooks/                # React хуки
├── types/                # TypeScript типы
└── supabase/             # SQL скрипты
    ├── schema.sql        # Схема БД
    └── apply-storage-policies.sql
```

## 🚢 Развертывание

### 📖 Подробная инструкция

Смотрите **[DEPLOY.md](./DEPLOY.md)** для пошагового руководства по деплою на Netlify.

### Быстрый старт

1. **Подготовьте код:**
   ```bash
   npm run build  # Проверьте, что сборка проходит
   git add .
   git commit -m "Готово к деплою"
   git push
   ```

2. **Деплой на Netlify:**
   - Зарегистрируйтесь на [netlify.com](https://www.netlify.com)
   - Подключите GitHub репозиторий
   - Добавьте переменные окружения (см. [DEPLOY.md](./DEPLOY.md))
   - Запустите деплой

3. **Переменные окружения (обязательно):**
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `NEXT_PUBLIC_SITE_URL` (опционально, но рекомендуется)

### Альтернатива: Vercel

1. Подключите репозиторий к [Vercel](https://vercel.com)
2. Добавьте те же переменные окружения
3. Деплой произойдёт автоматически

## 🔒 Безопасность

- ✅ **RLS (Row Level Security)** включен для всех таблиц
- ✅ **Валидация файлов**: размер (<5MB), тип, безопасное имя
- ✅ **Валидация URL** для link-экспонатов
- ✅ **Zod схемы** для всех форм
- ⚠️ **CORS**: Настройте в Supabase Dashboard → Settings → API

## 📊 Производительность

- ✅ Индексы БД для частых запросов
- ✅ ISR (Incremental Static Regeneration) для публичных страниц (revalidate: 60 сек)
- ✅ Оптимизация изображений через next/image
- ✅ Lazy loading для экспонатов ниже fold
- ✅ Кэширование настроено в next.config.js

## 🧪 Тестирование

Перед деплоем протестируйте:

- [ ] Регистрация и вход (email/password и GitHub)
- [ ] Создание музея с 10+ экспонатами
- [ ] Публичная страница на мобильном устройстве
- [ ] Шеринг в соцсети (проверьте Open Graph теги)
- [ ] Загрузка файлов разных размеров
- [ ] Валидация форм (попробуйте отправить невалидные данные)

## 📝 Переменные окружения

| Переменная | Описание | Обязательно |
|-----------|----------|-------------|
| `NEXT_PUBLIC_SUPABASE_URL` | URL проекта Supabase | ✅ |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Anon/Public ключ | ✅ |
| `SUPABASE_SERVICE_ROLE_KEY` | Service Role ключ (только для сервера) | ✅ |
| `NEXT_PUBLIC_SITE_URL` | URL сайта (для OAuth и SEO) | ⚠️ Рекомендуется |

## 🐛 Решение проблем

### Ошибка "Bucket not found"
- Убедитесь, что создали buckets `museum-covers` и `artifacts` в Supabase Storage
- Проверьте, что buckets публичные

### Ошибка 401 при авторизации
- Проверьте правильность `NEXT_PUBLIC_SUPABASE_ANON_KEY` в `.env.local`
- Убедитесь, что сервер перезапущен после изменения `.env.local`
- Очистите cookies браузера

### Ошибка RLS при загрузке файлов
- Выполните `supabase/apply-storage-policies.sql` в SQL Editor

## 📄 Лицензия

MIT

## 🤝 Вклад

Приветствуются Pull Requests! Для больших изменений сначала откройте Issue.

## 📧 Контакты

Для вопросов и предложений создайте Issue в репозитории.
