# 🔄 План миграции с Supabase на альтернативы

## 🎯 Цель
Заменить Supabase на решения, которые хорошо работают в России:
- **База данных**: PostgreSQL (напрямую или через Beget)
- **Аутентификация**: NextAuth.js (работает с PostgreSQL)
- **Хранилище файлов**: Локальное хранилище на сервере Beget

---

## 📋 Что нужно заменить

### 1. База данных
- **Было**: Supabase PostgreSQL (через @supabase/supabase-js)
- **Будет**: PostgreSQL напрямую через `pg` или `prisma`

### 2. Аутентификация
- **Было**: Supabase Auth (Email/Password + GitHub OAuth)
- **Будет**: NextAuth.js (Email/Password + GitHub OAuth)

### 3. Хранилище файлов
- **Было**: Supabase Storage
- **Будет**: Локальная папка на сервере + API routes для загрузки

---

## 🛠️ Технологический стек после миграции

- **Frontend**: Next.js 14 (App Router) + TypeScript + Tailwind CSS
- **База данных**: PostgreSQL (на Beget или отдельный сервер)
- **ORM**: Prisma (рекомендуется) или pg (напрямую)
- **Аутентификация**: NextAuth.js
- **Хранилище файлов**: Локальная папка `/public/uploads/`
- **Развертывание**: Beget

---

## 📦 Новые зависимости

```json
{
  "dependencies": {
    "next-auth": "^4.24.0",
    "@prisma/client": "^5.7.0",
    "prisma": "^5.7.0",
    "bcryptjs": "^2.4.3",
    "multer": "^1.4.5-lts.1",
    "@types/bcryptjs": "^2.4.6",
    "@types/multer": "^1.4.11"
  }
}
```

---

## 🔄 Шаги миграции

### Шаг 1: Установка PostgreSQL на Beget
1. Проверить доступность PostgreSQL на Beget
2. Создать базу данных
3. Применить схему из `supabase/schema.sql` (адаптировать под Prisma)

### Шаг 2: Настройка Prisma
1. Инициализировать Prisma
2. Создать схему на основе существующих таблиц
3. Настроить миграции

### Шаг 3: Замена аутентификации
1. Установить NextAuth.js
2. Настроить провайдеры (Email, GitHub)
3. Заменить все вызовы Supabase Auth на NextAuth.js

### Шаг 4: Замена хранилища файлов
1. Создать API route для загрузки файлов
2. Настроить multer для обработки файлов
3. Заменить FileUploader компонент

### Шаг 5: Замена запросов к БД
1. Создать Prisma клиент
2. Заменить все Supabase запросы на Prisma запросы
3. Обновить все компоненты

### Шаг 6: Тестирование
1. Проверить регистрацию/вход
2. Проверить создание музеев
3. Проверить загрузку файлов
4. Проверить публичные страницы

---

## ⚠️ Важные моменты

1. **Безопасность**: 
   - Хеширование паролей через bcryptjs
   - Валидация файлов (размер, тип)
   - Защита API routes

2. **Производительность**:
   - Индексы в БД (уже есть в schema.sql)
   - Кэширование запросов
   - Оптимизация изображений

3. **Хранилище файлов**:
   - Локальная папка на сервере
   - Резервное копирование
   - Ограничение размера файлов

---

## 📝 Переменные окружения после миграции

```env
# База данных
DATABASE_URL=postgresql://user:password@localhost:5432/kunstkamera

# NextAuth.js
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=your-secret-key-here

# GitHub OAuth (опционально)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Email (для отправки писем)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_password

# Production
NODE_ENV=production
PORT=3000
```

---

## 🚀 Готовы начать миграцию?

Это большая задача, но она решит проблему с доступностью Supabase в России.

**Время выполнения**: ~2-4 часа работы

**Сложность**: Средняя-Высокая

---

## ❓ Вопросы перед началом

1. Есть ли доступ к PostgreSQL на Beget?
2. Нужен ли GitHub OAuth или достаточно Email/Password?
3. Какой размер файлов планируется хранить? (для выбора стратегии)

